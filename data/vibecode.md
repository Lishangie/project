# Вайбкодинг: почему знания важнее инструментов

> **Сложность:** Простой | **Время чтения:** 7 мин | **Просмотры:** 15K
>
> **Категории:** Искусственный интеллект, Программирование | **Тип:** Мнение

Вайб-кодинг стал **словом 2025 года** по версии Collins Dictionary. **80% российских разработчиков** уже попробовали этот подход, а **четверть стартапов** в Y Combinator имеют кодовую базу, на **95% сгенерированную ИИ**. Но за красивыми цифрами скрывается неудобная правда: вайб-кодинг — это не волшебная палочка для тех, кто не умеет программировать, а **мощный инструмент**, эффективность которого напрямую зависит от знаний пользователя.

## Что такое вайб-кодинг и почему о нём все говорят

В **феврале 2025 года** Андрей Карпатый, бывший директор по ИИ в Tesla и сооснователь OpenAI, опубликовал твит, который изменил индустрию:  
"> "Существует новый способ писать код, который я называю «вайб-программированием», когда вы полностью погружаетесь в поток и забываете, что код вообще существует. Я прошу сделать самые примитивные вещи вроде «уменьши размер боковой панели в два раза», потому что мне лень искать это место в коде.""

**Суть проста:** описываешь задачу словами — ИИ пишет код.

> **"Программирование атмосферой, а не переменными"** — Collins Dictionary

**Низкий порог входа** и **скорость создания MVP** сделали подход популярным. Основные инструменты:

- 🔧 **Cursor**
- 🌊 **Windsurf** (приобретена OpenAI за $3 млрд)
- 🤖 **Claude Code**
- 📝 **GitHub Copilot**

Но давайте посмотрим, что происходит, когда этими инструментами пользуются люди с разным уровнем подготовки.

## Две стороны медали: примеры из практики

---

### Пример 1: SQL-инъекция в форме авторизации

Представим типичную ситуацию. Новичок просит ИИ: **"Сделай форму входа с проверкой логина и пароля из базы данных"**.

#### ❌ Что генерирует ИИ:

    ```
    def login(username, password):
        query = f"SELECT * FROM users WHERE username='{username}' AND password='{password}'"
        result = db.execute(query)
        if result:
            return "Успешный вход!"
    ```

**Реакция новичка:** "Отлично, работает! ИИ за 5 секунд написал то, на что у меня ушло бы полчаса!"

**Реакция профессионала:** "⚠️ Стоп, это же классическая **SQL-инъекция**! Любой может войти с `username admin' OR '1'='1' --`. Согласно [исследованию NYU Tandon School](https://example.com), в **40% случаев** Copilot генерирует код с уязвимостями."

#### ✅ Правильное решение:

    ```
    def login(username, password):
        # Используем параметризованные запросы
        query = "SELECT * FROM users WHERE username = %s AND password = %s"
        # И конечно, пароль должен быть хеширован!
        result = db.execute(query, (username, hash_password(password)))
    ```

---

### Пример 2: XSS в выводе пользовательских данных

#### ❌ ИИ генерирует:

    ```
    // Промпт: "Выведи комментарии пользователей на страницу"
    function displayComment(comment) {
        document.getElementById('comments').innerHTML +=
            '<div class="comment">' + comment.text + '</div>';
    }
    ```

Новичок: "Круто, комментарии отображаются!"

Профессионал: "⚠️ А если в `comment.text` будет `<img src=x onerror=\"alert('XSS')\">`? [Исследование Veracode](https://example.com) показывает, что **45% созданного ИИ кода** содержит классические уязвимости из **OWASP Top 10**."

#### ✅ Безопасное решение:

    ```
    function displayComment(comment) {
        const div = document.createElement('div');
        div.className = 'comment';
        div.textContent = comment.text; // textContent автоматически экранирует HTML
        document.getElementById('comments').appendChild(div);
    }
    ```

---

### Пример 3: Работа с устаревшими API

Реальный кейс из статьи **"Вайб-кодинг глазами старого разработчика"** на Хабре.
Автор использовал ИИ для работы с **Bitrix24 API**:

#### ❌ ИИ генерирует:

```javascript
// Устаревший метод API
async function getTasks() {
  const response = await fetch('/api/task.item.list');
  const tasks = await response.json();
  return tasks; // Где пагинация?
}
```

**Новичок:** "Работает медленно, но работает же!"

**Профессионал:**

- ⚠️ Используется **устаревший метод** `task.item.list` вместо `tasks.task.list`
- ⚠️ API Bitrix24 выдаёт списки **порционно по 50 ответов** за раз
- ⚠️ **Отсутствует обработка ошибок**

#### ✅ Правильный подход:

```javascript
async function getTasks(start = 0) {
  try {
    const allTasks = [];
    let hasMore = true;

    while (hasMore) {
      const response = await fetch(`/api/tasks.task.list?start=${start}`);
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

      const data = await response.json();
      allTasks.push(...data.result.tasks);

      hasMore = data.result.tasks.length === 50;
      start += 50;
    }
    return allTasks;
  } catch (error) {
    console.error('Ошибка при получении задач:', error);
    throw error;
  }
}
```

---

## Проблемы безопасности: цифры не врут

| Источник                 | Статистика                                                                 |
| ------------------------ | -------------------------------------------------------------------------- |
| NYU Tandon School (2025) | **40%** кода GitHub Copilot содержит уязвимости                            |
| Veracode (2025)          | **45%** ИИ-кода содержит уязвимости из OWASP Top 10                        |
| Wiz (2025)               | **20%** приложений, созданных вайб-кодингом, содержат серьёзные уязвимости |

### Реальные последствия:

- 🔴 **Fortnite** — SQL-инъекция через вайб-код → утечка аккаунтов
- 🔴 **Tea** — две крупные утечки данных из-за ИИ-кода
- 🔴 **CVE-2025-55284** — уязвимость в Claude Code позволяла извлекать данные с компьютера разработчика

---

## Симптомы "уставшей модели"

После **20–30 итераций** правок модель начинает вести себя странно.

#### ❌ Пример переполнения контекста:

```python
def process_data(data):
    # Обработка данных
    result = clean_data(data)

    # ❌ Модель упорно добавляет новый код сюда
    api_response = fetch_external_api()  # ????

    return result

def fetch_external_api():
    # ✗ А должна была добавить сюда
    pass
```

#### Признаки переполнения контекста:

- ⚠️ Вставляет код не в те места
- ⚠️ Повторяет ошибки
- ⚠️ Блуждает между решениями
- ⚠️ Забывает предыдущие исправления

---

## Архитектурная слепота

Вайб-кодинг генерирует всё в **одном файле**:

    ```html
    <!DOCTYPE html>
    <html>
    <head>
        <style>
            /* 500 строк CSS */
        </style>
    </head>
    <body>
        <div id="app"></div>
        <script>
            /* 1000 строк JavaScript */
            /* Вся бизнес-логика */
            /* Все обработчики */
        </script>
    </body>
    </html>
    ```

**Новичок:** "Удобно, всё в одном файле!"

**Профессионал:** "⚠️ Это **непригодно для поддержки**. Минимум **три файла** нужны: CSS, JS, HTML."

---

## Тестирование: невидимая граница между хаосом и порядком

### ❌ Иллюзия работоспособности:

    ```python
    def calculate_discount(price, discount_percent):
        return price * (1 - discount_percent / 100)

    # 'Проверил на калькуляторе — работает!'
    ```

### ✅ Профессиональный подход — тесты:

    ```
    def calculate_discount(price, discount_percent):
        """Расчёт цены со скидкой с валидацией"""
        if not isinstance(price, (int, float)) or price < 0:
            raise ValueError("Цена должна быть положительным числом")
        if not 0 <= discount_percent <= 100:
            raise ValueError("Скидка должна быть от 0 до 100%")
        return round(price * (1 - discount_percent / 100), 2)


    def test_calculate_discount():
        assert calculate_discount(100, 10) == 90.00
        assert calculate_discount(99.99, 15) == 84.99
        assert calculate_discount(100, 0) == 100
        assert calculate_discount(100, 100) == 0

        with pytest.raises(ValueError):
            calculate_discount(-100, 10)
        with pytest.raises(ValueError):
            calculate_discount(100, 150)
    ```

---

## Регрессия: опасность многократных исправлений

ИИ может **исправить одну ошибку** и **создать две новые**:

### Версия 1 (исходная):

    ```
    def process_payment(amount, card_number):
        if amount <= 0:
            return {"error": "Invalid amount"}
        return {"success": True}
    ```

### Версия 2 (с регрессией):

    ```python
    def process_payment(amount, card_number):
        if len(card_number) != 16:
            return {"error": "Invalid card"}
        # ❌ проверка amount исчезла!
        return {"success": True}
    ```

---

## Эволюция разработчика в эпоху ИИ

### Уровень 1: Новичок — 🌪️ хаос

Полагается на ИИ, не проверяет код, верит всему без вопросов.

### Уровень 2: Продвинутый — ⚡ ускорение

Использует ИИ как помощника, проверяет код, пишет тесты.

### Уровень 3: Профессионал — 🚀 максимальная продуктивность

Наставляет ИИ, интегрирует в CI/CD, обучает команду.

---

## Правильный подход к вайб-кодингу

### ✅ Когда использовать:

- 🎯 **Прототипирование** — быстрая валидация идеи
- 🛠️ **Генерация boilerplate** — repetitive code
- 🧪 **Тесты** — unit & integration тесты
- 📚 **Изучение библиотек** — примеры использования
- 🔄 **Рефакторинг с тестами** — при наличии coverage

### ❌ Когда НЕ использовать:

- 🔐 **Критичная логика** — система аутентификации, платежи
- 👤 **Персональные данные** — ПИД, медданные
- 🧮 **Сложные алгоритмы** — криптография, ML модели
- 🏭 **Продакшн без тестов** — никогда!

### 📋 Чек-лист перед использованием ИИ-кода:

- [ ] **Понимаю ли я, что делает код?**
- [ ] **Есть ли тесты?**
- [ ] **Проверил на уязвимости?** (OWASP Top 10)
- [ ] **Соответствует архитектуре?**
- [ ] **Учтены граничные случаи?**
- [ ] **Код понятен другим разработчикам?**

---

## Взгляд в будущее

**25% стартапов Y Combinator 2025 года** — **95% кода сгенерировано ИИ**.

Но **знания важнее инструментов**.

### Мнение экспертов:

> **Потёмин:** "ИИ ускоряет, но **не заменяет навыки**."

> **ICT.Moscow:** "Программирование — это **логика**. ИИ не справится **без человека**."

---

## Заключение

**Вайб-кодинг усиливает сильных и ломает слабых.**

- 🚀 Это не магия.
- 📈 Это умножитель, который работает только при **наличии знаний**.

> **"Вайб-кодинг без знаний — как вождение с закрытыми глазами по навигатору."**

---

### 📌 P.S.

Автор проверил **каждую строку кода** и **каждую статистику**.

✋ **Ответственность — всегда на разработчике.**
